# Adaptive Web Scraper Agent

An AI agent that scrapes websites and automatically fixes itself when layouts change.

## How to Run

1. Install: pip install requests beautifulsoup4 anthropic
2. Set API key: set ANTHROPIC_API_KEY=your-key-here
3. Run: python agent.py
